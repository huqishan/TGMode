<UserControl x:Class="Module.Test.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Module.Test.Views"
             mc:Ignorable="d"
             d:DesignHeight="768"
             d:DesignWidth="1366">
    <Grid>
        <ScrollViewer x:Name="StationsScrollViewer"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled"
                      SizeChanged="StationsScrollViewer_SizeChanged">
            <ItemsControl Height="{Binding ActualHeight, ElementName=StationsScrollViewer}"
                          ItemsSource="{Binding Stations}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="{x:Type ContentPresenter}">
                        <Setter Property="Margin" Value="0,0,12,0" />
                        <Setter Property="Height" Value="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:TestMaxView Width="{Binding DataContext.StationItemWidth, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                           Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType={x:Type ContentPresenter}}}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
